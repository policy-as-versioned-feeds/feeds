# The bump declared for this feed's next release (ticket 04 answer point 3;
# ADR-0019). The scheduled fetch rewrites this line in the PR it opens; a
# human reviews it against what bump.py computed. "none" means nothing is
# queued for release.
#
# SET TO `major` 2026-09-09 (eco-system ticket 79 item 4). threat-register/v3/
# is in the tree and untagged: the queued release is v3.0.0 and it is a major,
# because the payload schema changed -- `lm_gbp` and `lm_basis` per institution,
# and `lef_basis` beside them (ADR-0019).
#
# THE CUT IS NOT DONE HERE. cut-release.yml is dispatched by the owner and it is
# the owner who signs threat-register/v3.0.0. Nothing re-prices before that tag:
# driftwood pins v2 and tuppence and ludlow pin v1, and all three keep pricing
# the same magnitudes either way, because major 3's numbers ARE the numbers the
# platform's own table held.
# RESET TO `none` 2026-09-10: threat-register/v3.0.0 is tagged, signed and
# published, so nothing is queued, which is what `none` means above. The prose
# is kept rather than deleted, because it records what v3.0.0 was and why.
#
# SET TO `major` 2026-09-26 (eco-system ticket 145, ADR-0031). threat-register/v4/
# is in the tree and untagged: the queued release is v4.0.0 and it is a major,
# because the payload schema changed (payload.schema.v4.json): each institution
# gains a `threats` map beside its headline threat, and the one row in it,
# `scheduled-agent-misuses-write-credential`, is the frequency the platform's
# twin-agent cage price reads. bump.py computes `major` for v3 -> v4 under
# rule.yaml (payload_schema changed). The headline threat, its lef, lm_gbp and
# both bases are byte-for-byte v3's, so the move changes no headline price.
#
# THE CUT IS NOT DONE HERE. cut-release.yml is dispatched by the owner and it is
# the owner who signs threat-register/v4.0.0. Nothing re-prices before that tag:
# driftwood pins v2 and tuppence and ludlow pin v1, and each adopter's twin-agent
# line is a named could-not-look until its pin moves to v4.
bump: major
