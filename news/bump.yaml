# The bump declared for this feed's next release (ticket 04 answer point 3;
# ADR-0019). The scheduled fetch rewrites this line in the PR it opens: minor
# for a newly admitted event, major for a withdrawal, patch for a restatement.
# "none" means nothing is queued for release.
bump: none
